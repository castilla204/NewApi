# NewApi

API en C# (.NET) del ecosistema **DealRadar**: anuncios, scoring y gateway.

Expone los servicios REST que consumen el frontend `ReactWeb` y la app `dealradar` (Flutter).

**Stack:** C# · .NET / ASP.NET Core · RabbitMQ · MongoDB
